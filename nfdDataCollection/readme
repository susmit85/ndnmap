To build this directory:
	./waf configure --with-c++11
	./waf

-------------
Examples:
-------------
To run the client:
	build/nfdstat_c -p /ndn/edu/arizona/ndnmap/stats -d 1

To run the server:
	 build/nfdstat_s -f linksListExample -n 4 -d 1
